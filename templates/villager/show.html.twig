{% extends 'base.html.twig' %}

{% block title %}Villager{% endblock %}

{% block body %}
    <h1>Villager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ villager.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ villager.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ villager.type }}</td>
            </tr>
            <tr>
                <th>Ugliness</th>
                <td>{{ villager.ugliness }}</td>
            </tr>
            <tr>
                <th>Image_url</th>
                <td>{{ villager.imageUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_villager_index') }}">back to list</a>

    <a href="{{ path('app_villager_edit', {'id': villager.id}) }}">edit</a>

    {{ include('villager/_delete_form.html.twig') }}
{% endblock %}
