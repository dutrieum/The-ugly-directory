<header class="bg-light-green py-3 px-24 text-30" style="box-shadow: 0px 5px 12px rgba(0, 0, 0, 0.25);">
    <div class="flex justify-between items-center">
        <div class="">
            <a href="">
                <img src="{{ asset('/images/logo.svg') }}" alt="Logo" />
            </a>
        </div>
        <div class="gap-x-[30px] flex">
            <a href="{{ path("app_villager_index") }}">Villagers</a>
            {% if is_granted('ROLE_USER') %}
                <a href="">Logout</a>
            {% else %}
                <a href="">Login</a>
            {% endif %}
        </div>
    </div>
</header>